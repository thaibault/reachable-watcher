{
  "name": "reachable-watcher",
  "version": "1.0.77",
  "license": "CC-BY-3.0",
  "description": "Check status codes of web servers and sends notification e-mails",
  "homepage": "https://torben.website/reachableWatcher",
  "documentationWebsite": {
    "name": "reachableWatcher",
    "trackingCode": "UA-40192634-15"
  },
  "keywords": [
    "reachable",
    "watcher",
    "bash",
    "status",
    "code",
    "checker",
    "online"
  ],
  "files": [
    "reachableWatcher.service"
  ],
  "main": "reachableWatcher.sh",
  "bin": {
    "reachableWatcher": "reachableWatcher.sh"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thaibault/reachableWatcher"
  },
  "scripts": {
    "clear": "rm --recursive apiDocumentation &>/dev/null || true",
    "document": "mkdir --parents apiDocumentation/reachableWatcher/latest && bashlink-document reachableWatcher 1>apiDocumentation/reachableWatcher/latest/index.html",
    "document:show": "bashlink-document reachableWatcher",
    "document:show:error": "bashlink-document reachableWatcher 1>/dev/null",
    "lint": "hash shellcheck && shellcheck -x *.sh || echo Please install \"shellcheck\". || true",
    "lint:given": "shellcheck -x",
    "test": "bashlink-doctest --verbose ../../reachableWatcher",
    "update": "yarn version --patch && yarn publish",
    "update:documentation": "documentationWebsite --log-level info"
  },
  "devDependencies": {
    "bashlink": "*",
    "documentation-website": "*",
    "shellcheck": "*"
  }
}
